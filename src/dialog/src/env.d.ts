/// <reference types="vite/client" />

// Replaced at build time by Vite's `define`. Sourced from the extension's
// manifest.json so the About box matches the installed .ablx version.
declare const __APP_VERSION__: string;
